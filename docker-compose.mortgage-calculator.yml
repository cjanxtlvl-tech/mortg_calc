services:
  mortgage-calculator:
    build:
      context: .
      dockerfile: Dockerfile.mortgage-calculator
    image: mortgage-calculator:latest
    ports:
      - "8080:80"
    restart: unless-stopped
